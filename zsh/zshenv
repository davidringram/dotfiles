# .zshenv is loaded on EVERY zsh invocation. 
# Keep it fast and avoid any output (echo).

# Utility function to check if a command exists
# Useful for conditional loading in other files
function exists() {
  command -v "$1" >/dev/null 2>&1
}

