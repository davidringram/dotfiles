#Dotbot configuration file
- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.gitconfig:
    ~/.config/starship.toml:
    ~/.hyper.js:

- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
