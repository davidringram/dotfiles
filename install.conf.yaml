#Dotbot configuration file
- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.config/starship.toml:
    ~/.hyper.js:

- create:
    - ~/School

- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
