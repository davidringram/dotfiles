- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: 
    ~/.gitconfig: 
    ~/.config/starship.toml: 

- create:
    - ~/School

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
